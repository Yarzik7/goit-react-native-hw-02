import { Image, Text, View } from 'react-native';

const PostProperty = ({ Icon, caption }) => {
  return (
    <View>
      <Image />
      <Text />
    </View>
  );
};

export default PostProperty;
